import FilterListIcon from '@material-ui/icons/FilterList';
import { styled } from '@material-ui/styles';

const FilterListIconWithLeftMargin = styled(FilterListIcon)({ marginLeft: 8 });

export default FilterListIconWithLeftMargin;