import File from './../../language/stencilForm/File';

const file: File = {
    noFileIsSelected: 'No file is selected',
    fileArchive: 'File archive',
    selectFile: 'Select file',
    changeFile: 'Change the selected file'
};

export default file;